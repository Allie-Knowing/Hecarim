enum uri {
  video_answer = "/answer/video",
  comment_answer = "/answer/comment",
  googleSignin = "/google",
  like = "/like",
  file = "/file",
    auto_complete = "/search/autocomplete"
}

export default uri;
