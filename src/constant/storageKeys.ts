const storageKeys = {
  refreshToken: "refresh_token",
  accessToken: "access_token",
  isFirstQuestionUpload: "is_first_question_upload",
  adModalCloseAt: "ad_modal_close_at",
} as const;

export default storageKeys;
